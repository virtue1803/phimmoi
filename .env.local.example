# Lấy API key tại https://www.themoviedb.org/settings/api
# Biến này chỉ dùng ở phía server (route /api/tmdb), KHÔNG đặt tiền tố NEXT_PUBLIC_
# vì mọi biến NEXT_PUBLIC_ đều bị nhúng vào bundle và lộ ra trình duyệt.
TMDB_API_KEY=
